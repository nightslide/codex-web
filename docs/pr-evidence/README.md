# Pull request screenshots

Actual browser captures, cropped to exclude sidebar connections, task history, account details, and personal paths. The visible project and selected folder use demo names.

- `folder-before.png`: upstream main at `0dfdc10768c724d9a6ba507a93c3d6314ef073d8`, source-folder button does not open a picker.
- `folder-after.png`: working browser picker with a demo directory.
- `folder-selected.png`: one folder returned to the project form; the form was subsequently cancelled.

Folder after captures use the integrated local build containing both fixes. Each proposed code branch was built and tested separately. No personal project was created for these screenshots.
